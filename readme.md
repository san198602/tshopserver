## mongodb://localhost:27017



rm -rf node_modules
rm -f package-lock.json # or yarn.lock if using Yarn
npm cache clean --force
npm install

rm -rf node_modules
rm package-lock.json # or yarn.lock
npm install # or yarn install

.
├── src/
│   ├── config/             # Configuration files (e.g., database, environment variables)
│   ├── controllers/        # Handle incoming requests and orchestrate application logic
│   ├── interfaces/         # TypeScript interfaces and types
│   ├── middleware/         # Custom middleware functions (e.g., authentication, validation)
│   ├── models/             # Database models (e.g., Mongoose schemas, Sequelize models)
│   ├── routes/             # Define API routes and map them to controller methods
│   ├── services/           # Encapsulate business logic, keeping controllers clean
│   ├── utils/              # Utility functions (e.g., helpers, logging)
│   ├── validators/         # Request validation logic
│   ├── index.ts            # Application entry point
│   └── server.ts           # Server setup and configuration
├── tests/                  # Unit and integration test files
├── dist/                   # Compiled JavaScript output (generated by TypeScript)
├── .env                    # Environment variables (local)
├── .gitignore              # Files/directories to ignore in Git
├── package.json            # Project metadata and dependencies
├── tsconfig.json           # TypeScript compiler configuration
└── README.md               # Project documentation